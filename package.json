{
  "name": "tenshu",
  "version": "0.1.0",
  "private": true,
  "description": "Real-time dashboard for OpenClaw AI agent teams",
  "workspaces": [
    "shared",
    "server",
    "client"
  ],
  "scripts": {
    "dev": "concurrently -n server,client -c green,blue \"npm run dev -w server\" \"npm run dev -w client\"",
    "build": "npm run build -w server && npm run build -w client",
    "lint": "npm run lint --workspaces --if-present",
    "test": "npm run test -w shared && npm run test -w server && npm run test -w client",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "eslint-config-prettier": "^10.1.8",
    "prettier": "^3.8.1",
    "typescript": "^5.7.0"
  }
}
