import type { TenshuConfig } from '@tenshu/shared'

const config: TenshuConfig = {
  openclawDir: '~/.openclaw',
  port: 3001,
  clientPort: 5173,
  theme: 'dark',
  accentColor: '#ff6b35',
}

export default config
